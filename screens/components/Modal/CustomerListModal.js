import React from 'react';
import {View, Text} from 'react-native';
import Modal from 'react-native-modal';

const CustomerListModal = ({isVisible}) => {
  console.log(isVisible);
  return <View></View>;
};

export default CustomerListModal;
